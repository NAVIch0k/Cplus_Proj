#include <chrono>
#include <iostream>
using namespace std;
using namespace chrono;
int main()
{
    cout << "begin\n";
    steady_clock::time_point tend = steady_clock::now() + minutes(1);
    while (steady_clock::now() < tend)
    {
        // do your game
    }
    cout << "end\n";
}